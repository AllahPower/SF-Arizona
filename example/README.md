# Examples

Примеры внешних модулей для `SF`, которые собираются отдельно от `SF.Runtime` и ссылаются только на `SF.Abstractions`.

- `HelloWorld` - минимальный модуль, который пишет `Hello world!` в чат и в стандартный SF-лог.
