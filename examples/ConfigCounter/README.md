# Config Counter Example

This example shows an external module built on the direct `ISFModule` contract.

It stores a start counter in module config, exposes `/counterexample`, and demonstrates lifecycle hooks together with `Context.Config` and `Context.UserData`.
