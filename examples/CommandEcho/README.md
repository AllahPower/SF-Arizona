# Command Echo Example

This example shows a long running external module built on the direct `ISFModule` contract.

It registers `/echoexample`, writes command input to chat and to the SF log, and stops cleanly when the module is stopped or the plugin is unloaded.
